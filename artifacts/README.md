# Artifacts

Small reference result tables are kept here so the notebook can reproduce headline figures quickly.

Large feature matrices, frozen models and other heavy artifacts should live in Google Drive under:

`MyDrive/FlexFactor_Final_Project/artifacts/`
